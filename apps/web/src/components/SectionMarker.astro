---
interface Props {
  number: string;
  label?: string;
  class?: string;
}

const { number, label, class: className = '' } = Astro.props;
---

<div class={`flex items-center gap-3 font-ui text-xs uppercase tracking-widest ${className}`}>
  <span class="font-semibold">N°{number}</span>
  {label && <span class="opacity-70">{label}</span>}
</div>
